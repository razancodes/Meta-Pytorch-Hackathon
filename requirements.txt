# ═══════════════════════════════════════════════════════════
# Memex OS-Agent Benchmark — Python Dependencies
# ═══════════════════════════════════════════════════════════

# ── Core Runtime (required for OpenEnv server + client) ──
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
httpx>=0.25.0
openai>=1.6.0
numpy>=1.24.0

# ── OpenEnv SDK ──
openenv-core

# ── Training (GRPO + Self-Play, GPU required) ──
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# trl>=0.7.0
# peft>=0.6.0
# bitsandbytes>=0.41.0
# datasets>=2.14.0
# wandb
# accelerate>=0.24.0
# matplotlib

# ── AgentOS-Kernel Production (A100 80GB, optional) ──
# vllm>=0.4.0
# transformers>=4.37.0
# sentence-transformers>=2.2.0
# tiktoken>=0.5.0
# lancedb>=0.4.0
# pyarrow>=14.0.0
# maturin>=1.3.0
